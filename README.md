Pour démarrer le projet la première fois, executer la commande suivante :
```bash
docker-compose up --build
```

Une fois le projet installer, vous pourrez alors redemarrer le projet avec la commande :
```bash
docker-compose up
````